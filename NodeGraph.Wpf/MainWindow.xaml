<Window x:Class="NodeGraph.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NodeGraph.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="NodeGraph WPF Demo" Height="450" Width="800">

    <!-- 이 창의 DataContext를 MainViewModel로 설정 -->
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1행: 결과 값 -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Result, StringFormat=Result: {0}}"
                   FontSize="16"
                   Margin="0 0 0 10"/>

        <!-- 2행: 노드 목록 + 포트 정보 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Nodes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:NodeViewModel}">
                        <Border BorderBrush="Gray" BorderThickness="1"
                                CornerRadius="4" Padding="8" Margin="0 0 0 8">
                            <StackPanel>
                                <!-- 노드 이름 (지금은 DisplayName = Id) -->
                                <TextBlock Text="{Binding Name}"
                                           FontWeight="Bold"/>

                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <!-- 입력 포트 -->
                                    <StackPanel Margin="0,0,20,0">
                                        <TextBlock Text="Inputs" FontWeight="SemiBold"/>
                                        <ItemsControl ItemsSource="{Binding Inputs}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <!-- 출력 포트 -->
                                    <StackPanel>
                                        <TextBlock Text="Outputs" FontWeight="SemiBold"/>
                                        <ItemsControl ItemsSource="{Binding Outputs}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
